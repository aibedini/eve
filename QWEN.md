# Agent instructions

Read and follow `AGENTS.md`. In particular, the `codebase-memory-mcp` graph-first workflow in `docs/AI_CODEBASE_MEMORY.md` is mandatory for every coding task. Do not default to broad grep or recursive file reads.
## UI changes

For every user-facing UI change, read and follow `.agents/skills/eve-ui/SKILL.md`.
`static/style.css` and `templates/base.html` remain the implementation source of truth.
Do not introduce a parallel visual system. See `docs/UI_DESIGN_SYSTEM.md`.

