# Memory Diagnostics Contract

The memory report adds bounded `background_fetch` samples containing only cycle id,
checkpoint, timestamp, PSS/USS bytes, raw inbound/client counts, processed rows, inflight
tasks, and retained server results. Credentials, commands, addresses, account values,
emails, UUIDs, URLs, comments, and raw payloads are forbidden.
