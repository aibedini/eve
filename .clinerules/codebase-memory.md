# Mandatory Codebase Memory workflow

Follow `AGENTS.md` and `docs/AI_CODEBASE_MEMORY.md`. Use `codebase-memory-mcp` first for every coding task; raw filesystem search is limited to the fallback cases documented there.
